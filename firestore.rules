rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Sincronização fabril Home Pots
    match /homepots_sync/{docId} {
      allow read, write: if true;
    }

    match /{document=**} {
      allow read, write: if true;
    }
  }
}

import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.homepots.manager',
  appName: 'Home Pots',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
